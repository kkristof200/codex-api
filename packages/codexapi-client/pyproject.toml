[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "codexapi-client"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "websocket-client>=1.8.0",
]

[tool.setuptools.packages.find]
where = ["src"]
